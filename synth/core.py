def generate_dump(db_url, rows, seed, out_file, verbose):
    """Stub: implement schema introspection and data generation here."""
    print(
        f'Called generate_dump with db={db_url}, rows={rows}, seed={seed}, out={out_file}, verbose={verbose}'
    )
